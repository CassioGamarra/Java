package view;

public class InicializarView {
    public static void main(String[] args) {
        ViewLogin view = new ViewLogin();
        view.setLocationRelativeTo(null);
        view.setVisible(true);
    }
}
